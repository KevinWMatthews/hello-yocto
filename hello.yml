---
header:
  version: 14
machine: qemux86-64
distro: poky
target: core-image-minimal
local_conf_header:
  hello: IMAGE_INSTALL:append = " hello-c"
  debug: |
    EXTRA_IMAGE_FEATURES = "debug-tweaks"
repos:
  poky:
    url: https://git.yoctoproject.org/poky
    branch: scarthgap
    layers:
      meta:
      meta-poky:
      meta-yocto-bsp:
  meta-hello:
    path: meta-hello
